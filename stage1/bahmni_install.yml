---

- hosts: localhost
  connection: local
  vars_files:
    - variables.yml
    - variables_private.yml
    - "variables_{{ lookup('env', 'impl_file_suffix') }}.yml"
  roles:
    - fix_python
    - setup_installer

